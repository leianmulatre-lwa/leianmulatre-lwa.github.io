(()=>{const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const directory=()=>{try{const v=JSON.parse(localStorage.getItem('biglwaUserDirectory')||'[]');return Array.isArray(v)?v.filter(u=>u&&String(u.username||'').trim()):[]}catch{return[]}};const room=()=>document.querySelector('#studioApp [data-widget-route="rooms"],#studioApp [data-widget-route="room"],#studioApp #rooms,#studioApp #room');const ensure=()=>{const card=room();if(!card)return;let box=card.querySelector('.biglwa-room-user-search');if(!box){box=document.createElement('div');box.className='biglwa-room-user-search';box.innerHTML='<label><span>Find a user</span><input type="search" autocomplete="off" spellcheck="false" placeholder="Search username…"></label><div class="biglwa-room-search-results" aria-live="polite" hidden></div>';const target=card.querySelector('.room-footer,.room-body,.card-body,.module-body')||card;target.prepend(box)}const input=box.querySelector('input'),results=box.querySelector('.biglwa-room-search-results');if(!input||input.dataset.biglwaBound==='1')return;input.dataset.biglwaBound='1';const render=()=>{const q=input.value.trim().replace(/^@/,'').toLowerCase();if(!q){results.hidden=true;results.replaceChildren();return}const matches=directory().filter(u=>String(u.username).toLowerCase().includes(q)).slice(0,12);results.hidden=false;if(!matches.length){results.innerHTML='<p class="biglwa-room-search-empty">No users found yet.</p>';return}results.innerHTML=matches.map(u=>'<button type="button" class="biglwa-room-user-result" data-username="'+esc(u.username)+'"><b>@'+esc(u.username)+'</b><span>'+esc(u.name||'')+'</span></button>').join('')};input.addEventListener('input',render);results.addEventListener('click',e=>{const b=e.target.closest('[data-username]');if(!b)return;window.dispatchEvent(new CustomEvent('biglwa:user-search',{detail:{username:b.dataset.username}}));const u=directory().find(x=>String(x.username).toLowerCase()===b.dataset.username.toLowerCase());if(u?.url)location.href=u.url)};render()};const run=()=>{ensure();requestAnimationFrame(ensure)};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',run,{once:true});else run();window.addEventListener('biglwa:module-open',run)})();